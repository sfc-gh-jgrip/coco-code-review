---
created: 2026-05-28T11:13:35.306Z
session: 78898a9f-b221-4c6e-ab0b-b68e160960dc
working_directory: /Users/jgrip/dev/coco-apps/coco-code-review
---

**Context**
I checked the repo layout, the current ignore rules in `.gitignore:1`, and the visible generated/local artifacts present in the working tree from the file inventory. This directory is not yet a git repo, so there is no current `git status` to inspect. The important finding is that `.gitignore` is directionally good for a Python project, but there are a few concrete cleanup gaps before first commit:
- `src/coco_pr_review/__pycache__/...` and `tests/**/__pycache__/...` are present on disk and should not be committed. They are already covered by `.gitignore:17`, so once git is initialized they should remain untracked.
- `.pytest_cache/...` is present on disk but is not ignored by the top-level `.gitignore`. That is a gap.
- `.claude/settings.local.json` is present and is not ignored by `.gitignore`; that is likely machine-local and should almost certainly be ignored before sharing.
- `.claude/skills/...` appears to be intentional repo content, not ephemeral output.
- `tmp/` is already ignored by `.gitignore:31`, so the design docs in `tmp/` will not be part of the first commit unless you explicitly change that.
- `CLAUDE.md` is ignored by `.gitignore:34`, so it also will not be included.

**Implementation steps**
1. Decide the publication strategy before initializing git:
- If you want a clean public history, create a feature branch immediately after `git init` and do all live GitHub Actions iteration there.
- Keep `main` clean; only merge or fast-forward a squashed final commit onto `main` later.
2. Fix ignore hygiene before the first commit:
- Update `.gitignore` to add at minimum `.pytest_cache/`, `.claude/settings.local.json`, and optionally broader Python/test artifacts if you want tighter defaults such as `.coverage`, `htmlcov/`, `.mypy_cache/`, `.ruff_cache/`, `.venv/`, `venv/`, `.python-version`.
- Re-check whether there are any other local-only files you do not want in the repo, especially under `.claude/`.
3. Initialize git locally:
- `git init`
- optionally rename the default branch to `main` immediately: `git branch -M main`
4. Inspect the first candidate commit carefully before staging:
- `git status --short --ignored`
- verify that ignored items stay out: `tmp/`, `CLAUDE.md`, `__pycache__/`, `.pytest_cache/`, `.claude/settings.local.json` if you added that rule
- verify that intended repo content remains visible: `README.md`, `pyproject.toml`, `src/...`, `tests/...`, and any committed `.claude/skills/...` docs you actually want to share
5. Stage only intended files for the initial commit:
- `git add .`
- immediately run `git status --short` and confirm no junk slipped in
6. Create the initial commit:
- Use a message that reflects the repo’s actual current state, for example `Initial import of coco-pr-review prototype`
- This first commit does not need to imply completeness; it should just be honest
7. Connect the existing GitHub remote:
- `git remote add origin <github-repo-url>`
- verify with `git remote -v`
8. Push the initial branch:
- `git push -u origin main` if you are putting the prototype on `main`
- better: create and push a feature branch first, e.g. `git checkout -b actions-bootstrap` then `git push -u origin actions-bootstrap`
9. After the repo is on GitHub, do the live integration work on that feature branch:
- add workflow YAML
- add runtime bootstrap / real `main()` implementation
- add any required secrets or permissions in GitHub repo settings
- push incremental commits as needed while testing actual `pull_request` and `issue_comment` flows
10. Test the live GitHub Actions path in the hosted repo:
- open a test PR
- verify the `pull_request` workflow triggers
- leave a maintainer comment containing `@coco-review`
- verify the `issue_comment` workflow triggers and respects the maintainer gate
- verify skip cases: draft PR auto-skip, skip label, non-maintainer comments
11. Once the feature is complete and validated, clean the history before sharing widely:
- if the branch is only yours, squash locally with interactive rebase or `git reset --soft <base>`
- rewrite to one clean commit or a small logical series
- push with `git push --force-with-lease`
12. Merge the cleaned branch to `main`:
- either merge the single squashed branch commit via PR
- or fast-forward `main` locally after review, then push
13. Optional final cleanup pass before public sharing:
- review repo settings, branch protection, Actions permissions, secrets names, README accuracy, and whether ignored design docs should remain absent or be promoted into committed documentation

**What happens after the first push**
The practical sequence after `push` is:
1. Create or continue on a dedicated feature branch for the GitHub Actions/bootstrap work.
2. Implement workflow YAML and the real bootstrap entrypoint.
3. Push incremental commits to that branch and validate in live GitHub Actions.
4. Repeat until the PR/comment-trigger flow works end-to-end.
5. Squash the branch history into a clean final commit.
6. Force-push the cleaned branch with `--force-with-lease`.
7. Merge that cleaned result into `main`.

**Verification**
Before first commit:
- `git status --short --ignored`
- confirm ignored: `tmp/`, `CLAUDE.md`, `__pycache__/`, `.pytest_cache/`, local `.claude` settings
- confirm tracked candidates are only real source/docs/tests

After first commit / before first push:
- `git show --stat --name-only HEAD`
- verify no generated files or local notes are in the commit

After adding remote:
- `git remote -v`
- verify it points to the intended GitHub repo

During live GitHub iteration:
- `python -m pytest /Users/jgrip/dev/coco-apps/coco-code-review -q`
- open a PR and check Actions run logs
- add `@coco-review` as a maintainer comment and verify comment-trigger execution
- verify non-maintainer or non-trigger comments do not dispatch

Before history cleanup:
- `git log --oneline origin/main..HEAD`
- confirm which commits will be squashed

After squash / force-push:
- `git log --oneline --decorate -n 10`
- verify the branch now contains the clean history you want

**Critical Files**
- `.gitignore` - Needs a small cleanup pass before first commit; currently misses at least `.pytest_cache/` and likely local `.claude` settings.
- `src/coco_pr_review/github_event.py` - Current event-dispatch layer that will later need real bootstrap wiring for live Actions runs.
- `src/coco_pr_review/__main__.py` - Module entrypoint that should become the workflow invocation target.
- `pyproject.toml` - Package metadata and the most likely place to add a console-script entrypoint later if desired.
- `README.md` - Should be updated once the GitHub Actions/bootstrap flow is real so the shared repo matches reality.